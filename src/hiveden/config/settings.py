class Config:
    app_directory = "/shares/apps"
    movies_directory = "/shares/movies"
    tvshows_directory = "/share/tvshows"
    backup_directory = "/shares/backups"
    docker_network_name = "hiveden-net"

config = Config()
